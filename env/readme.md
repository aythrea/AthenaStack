# Environment Configuration

This directory contains example environment definitions used by
AthenaStack deployments.

Only example files should be committed.

Real deployment environment files are maintained outside Git and
supplied by the deployment mechanism.

Current environments:

- production